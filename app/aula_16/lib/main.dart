import 'package:flutter/material.dart';

import 'bloc.dart';
// import 'provider.dart';

void main() {
  runApp(const MaterialApp(home: TelaBloc()));
  // runApp(const MaterialApp(home: TelaProvider()));
}
